{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}

<main class="container">
    <secrion class="row">
        <div class="col-12">

            <h1>TAKE-AWAY FLOWERS</h1>
            <h2>Vente de Fleurs à emporté</h2>
            <p>Je sait, c'est un concept...</p>

        </div>

        {# {{ dump() }} Faire un dump pour voir la liste des categories disponible dans cette vue !!  #}
            
        {% for cat in categories %}
            {% if cat.parent == null %}
                <div class="col-12">
                    <h2>{{ cat.name }}</h2>
                </div>
            {% endif %}
            <div class="col-4">
                <div class="card h-100">
                    <div class="card-body">
                        <h3 class="card-title">{{ cat.name }}</h3>
                        {# {{categories.name}} #}
                        {# <a href="{{ path('categories_list', {slug_name}) }}" class="btn btn-primary">Voir les produits</a> #}
                        {# <a href="#" class="btn btn-primary">Voir les produits</a> #}
                    </div>
                </div>
                
            </div>  
        {% endfor %}
        
    </secrion>
</main>




{% endblock %}
